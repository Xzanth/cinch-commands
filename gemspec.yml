name: cinch-commands
summary: Define multiple commands within a Cinch Plugin
description: |
  Allows defining multiple commands within a Cinch Plugin.
  Also provides a !help command with detailed output.

license: MIT
authors:
  - Postmodern
  - Xzanth
homepage: https://github.com/Xzanth/cinch-commands

dependencies:
  cinch: ~> 2.0

development_dependencies:
  rubygems-tasks: ~> 0.2
  rspec: ~> 2.4
  yard: ~> 0.7
